spring:

  datasource:
    url: jdbc:h2:mem:testdb-test
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    sql:
      init:
        mode: always

  h2:
    console:
      enabled: false